CREATE TABLE Bilett
(
    id INTEGER AUTO_INCREMENT,
    film VARCHAR(255) ,
    antall INTEGER,
    fornavn VARCHAR(255) ,
    etternavn VARCHAR(255),
    epost VARCHAR(255),
    telefon VARCHAR,
    PRIMARY KEY (id)
);
